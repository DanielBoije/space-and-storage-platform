// error codes are used to be able to check for specific error responses and
// handle them accordingly on the client side

enum ErrorCode {
  EMAIL_ALREADY_IN_USE = "Email already in use",
  // add more as relevant
}

export default ErrorCode;
